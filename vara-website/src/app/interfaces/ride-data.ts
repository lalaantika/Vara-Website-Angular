import { Timestamp } from 'rxjs/internal/operators/timestamp';

export interface RideData {
    adds_played: number;
    client_email: string;
    driverId: string;
    ride_authorized:  boolean;
    ride_client: string;
    ride_date: any;
}
